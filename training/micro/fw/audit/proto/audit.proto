syntax = "proto3";

package audit;

option go_package = "fw/audit/proto;audit";

service Audit {
	rpc Call(Request) returns (Response) {}
}

message Request {
	string name = 1;
}

message Response {
	string msg = 1;
}